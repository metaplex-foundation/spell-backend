[http_server]
port = 8080

[obj_storage]
endpoint = "http://127.0.0.1:9000"
region = "us-east-1"
access_key_id = "admin"
secret_access_key = "password"
bucket_for_json_metadata = "asset-metadata"
bucket_for_binary_assets = "asset-binary"

[database]
connection_url="postgres://postgres:1111@localhost:5432/solana"
min_connections = 1
max_connections = 1

[metrics]
enabled = false